Camera pitch gets inverted when crossing over world origin and does nothing when approaching border
Try moving XEvent loop to separate thread
Implement camera as a class
Implement player movement code into a class
Create builder class for cubes + chunks
ImGui for debugging
